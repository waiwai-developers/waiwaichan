version: "3.9"
x-base-service: &base-service
  build:
    context: ../../.
    dockerfile: environment/development/Dockerfile
  networks:
    - server-network
  environment:
    GOOGLE_APPLICATION_CREDENTIALS: /app/config/key.json
    NODE_ENV: development
    NODE_OPTIONS: "--max-old-space-size=256 --no-deprecation"
  volumes:
    - type: bind
      source: ../../config
      target: /app/config
    - type: bind
      source: ../../.
      target: /app
    - type: volume
      source: node_modules
      target: /app/node_modules
  mem_limit: 256m
  restart: unless-stopped
  tty: true
services:
  backend:
    <<: *base-service
    container_name: waiwai-backend
    depends_on:
      database:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    command: ["pnpm", "run", "dev"]
  scheduler:
    <<: *base-service
    container_name: waiwai-scheduler
    depends_on:
      database:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    command: ["pnpm", "run", "dev:scheduler"]
  migrate:
    <<: *base-service
    container_name: waiwai-migrate
    restart: "no"
    mem_limit: 512m
    depends_on:
      database:
        condition: service_healthy
    command:
      [
        "sh",
        "-c",
        "pnpm run migrate up && pnpm run seed up && pnpm run datafix up"
      ]
  database:
    container_name: waiwai-database
    image: mysql:8.0
    platform: linux/x86_64
    env_file:
      - ../../.env
    command: mysqld --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - server-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      retries: 10
      start_period: 60s
      timeout: 10s
networks:
  server-network:
    driver: bridge
volumes:
  mysql_data:
  node_modules:
