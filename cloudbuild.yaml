steps:
  - name: 'http://asia-northeast1-docker.pkg.dev//cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/image:$COMMIT_SHA', '.']
  - name: 'http://asia-northeast1-docker.pkg.dev//docker'
    args: ['push', 'gcr.io/$PROJECT_ID/image:$COMMIT_SHA']
  - name: 'http://asia-northeast1-docker.pkg.dev//cloud-builders/gcloud'
    args: ['run', 'deploy', 'image',
           '--image', 'gcr.io/$PROJECT_ID/image:$COMMIT_SHA',
           '--region', 'asia-northeast1',
           '--platform', 'managed']
images:
  - 'gcr.io/$PROJECT_ID/image:$COMMIT_SHA'
